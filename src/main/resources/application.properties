# application name
spring.application.name=indie91

# mongodb uri
spring.data.mongodb.uri=mongodb+srv://root:root@indie91.dzyc0nr.mongodb.net/indie?retryWrites=true&w=majority&appName=indie91

# Show all SQL queries from Spring Data JPA or MongoDB
spring.jpa.show-sql=true

# Enable debug logs from Spring Boot internals (optional)
#logging.level.org.springframework.web=DEBUG

# Set the root logging level (options: TRACE, DEBUG, INFO, WARN, ERROR)
#logging.level.root=INFO

# Set specific logging level for your application package
#logging.level.com.example.indie91=DEBUG

# Show date, thread, log level, and logger name
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n

# Log file output (optional)
#logging.file.name=logs/indie91.log

# Maximum log file size (e.g., 10MB)
#logging.file.max-size=10MB

# Max number of log backups to keep
#logging.file.max-history=5